tasks:
  - name: "express-server"
    script: "npm start"  # This assumes your Express app starts with "npm start"
    ports:
      - 3000

  - name: "websocket-server"
    script: "node server.js"  # Replace with the actual WebSocket server file
    ports:
      - 8080
